{
  "type": "exec",
  "description": "stream processing by simple JSON protocol. Each message between process is one line JSON message, and response like one line JSON object as well",
  "interface": "stream",
  "params": [
    {"name": "data", "type": "binary", "is_input": true}
  ],
  "output":  {"type": "flat.json"}
}